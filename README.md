# Lua-code-obfuscator
Your script is encrypted byte‑by‑byte with a fresh random key, then Base64‑packed into a tiny Luau loader with randomized variable names. The loader decrypts and runs the payload at runtime, and your original source never appears in plaintext. All encryption happens locally in your browser.
